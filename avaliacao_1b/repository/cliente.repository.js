const conn = require('../pg-connection');

//Consulta padrão, utilizada em todas os SELECT
const queryDefault = ``;

//Função para ajustar os atributos das clientes
function ajustaAtributos(rows) {
    //Ajusta atributos das fotos
    for (index in rows) {
        
    }

    return rows;
}
//Camada de Modelo
module.exports = {

    
}